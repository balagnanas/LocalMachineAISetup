# Microsoft Scout profile

This public profile contains only portable UI and behavior settings. Copy selected values from
`m-settings.example.json` into your local Scout settings after reviewing them for compatibility with
your Scout version.

The source machine's Scout skills, automations, MCP definitions, and metadata are excluded. They can
contain organization-specific routing, service references, local paths, or credentials that are not
suitable for a public repository.
